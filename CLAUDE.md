See [AGENTS.md](AGENTS.md).

Quick start: `macarchy help`, then `macarchy doctor`. Finish every change with
`macarchy verify`.

The one rule: `~/.aerospace.toml` (native AeroSpace commands only) and
`~/.config/skhd/skhdrc` (anything that launches a program) must stay disjoint,
or chords fire twice.
