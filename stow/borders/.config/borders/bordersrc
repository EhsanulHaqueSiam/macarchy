#!/bin/bash
# Generated by omarchy-mac-colors. JankyBorders execs this when `borders` is
# started with no arguments, so it MUST end by invoking borders with options.
options=(
  style=round
  width=6.0
  hidpi=off
  active_color=0xffe2dddc
  inactive_color=0xaa584e51
)
borders "${options[@]}"
